GenBridge TODO

Things to do. ONE LINE PER ITEM - keep it that way.
Measurements, reasoning and completed-work narrative go in findings.txt, NOT here.
Built-but-unchecked work goes in to-test.txt.

FROM CT
- Resize smoothness, as Claude described below


2. Erratic resizing                                                    
Three separate causes, and only one of them is the one you'd guess:    
a) checkSizeConstraint() only ever looks at width (gbEditor.mm:198-215)wanted comes from rect->r height is then recomputedfrom it. Drag the bottom edge and your requested height is discarded anreplaced by one derived fthe window snaps back andfights you vertically. It should fit the aspect box to whichever dimenshost actually changed.
                                                                       b) The content repaints oring a live resize the host moves the frame at display rate while our content updates 30×/s, so it visibly lags the window eerence from G2-Edit, whichredraws on demand in its own loop and so repaints in step with the drag. An override of -setFrameSize immediately fixes it.
                                                                            c) Two masters on the fraNSViewWidthSizable|NSViewHeightSizable (gbEditor.mm:107) and onSize()       explicitly setFrame:s. Aprtional adjustment when the parent changes and then onSize() overwrites it, with no guaranteed order — aframe of wrong geometry erop the autoresizing maskand let onSize() be the sole authority.                                     
Minor: the (int32) truncations in checkSizeConstraint let a repeated resize creep a pixel at a time. t never used — that's themissing half if the plug-in ever wants to request a size rather than accept one.

- If plugin can't find the USB core audio device, should indicate a not found and wait for it to become present. Definitely shouldn't pass through mic audio, which is does as-is